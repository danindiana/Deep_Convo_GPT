#include <iostream>
#include <vector>

// Define your data structures and helper functions here (e.g., tree node, At function, etc.)

// Algorithm 1: Returns the new value B'(x), if the label of node v is set to σ.
// Procedure evaluate-state(v, σ, x)
// Implementation of evaluate-state algorithm here...

// Algorithm 2: Returns the new value for At(p, x, y), for siblings x ⪯ y, if the label of node v is set to σ.
// Procedure evaluate-sequence(v, σ, p, x, y)
// Implementation of evaluate-sequence algorithm here...

// Algorithm 3: Returns the new value for Bt(q, y, x), for an ancestor x of a node y if the label of node v is set to σ.
// Procedure evaluate-path(v, σ, q, x, y)
// Implementation of evaluate-path algorithm here...

int main() {
    // Sample usage of the PRAM algorithms
    // Define your data structures and tree here...

    // Call the PRAM algorithms with appropriate arguments to evaluate state, sequence, and path

    return 0;
}
